---
"gated": minor
---

Add core request coalescing with `buildGate({ coalesce: true })` and an optional custom key
projection. Deprecate `dedupeHook`; it remains available through this major release and is planned
for removal in the next major release.
