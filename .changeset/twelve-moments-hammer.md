---
"gated": minor
---

Allow `identify` to be omitted from `buildGate` when every evaluator and batch call supplies `{ identity }`. Caller-identity factories require the identity in their call options.
