---
"gated": patch
---

Clarify the README: the React gate cache section now explains the per-evaluator buckets and the shared bucket for custom `useGate` functions, conditions come before the statements they guard, and passive sentences name their actor.
